"""
EGX Data Providers — Shadow evaluation layer.

This package contains isolated data providers that run alongside the
existing Yahoo Finance fallback.  No provider in this package may
replace the active data path until explicitly promoted.
"""
